--Library
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
--Window
local Window = Library.CreateLib("Booga Booga Reborn (Made by fal#0001)", "Midnight")
--Tabs
local Main = Window:NewTab("Main")
local Visual = Window:NewTab("Visual")
local Misc = Window:NewTab("Misc")
local Changelog = Window:NewTab("Changelog")
local Settings = Window:NewTab("Settings")
--Sections
local MainSection = Main:NewSection("N/A")
local VisualSection1 = Visual:NewSection("Visual (Player)")
local VisualSection2 = Visual:NewSection("Visual (Ore)")
local VisualSection3 = Visual:NewSection("Visual (God)")
local MiscSection = Misc:NewSection("Misc")
local ChangelogSection = Changelog:NewSection("Changelog")
local SettingSection = Settings:NewSection("Settings")
--MainSection
--nothing here rn

--VisualSection1
VisualSection1:NewButton("Player Esp", "Player Esp", function()
    local camera = workspace.CurrentCamera
    local players = game:GetService("Players")
    local localplayer = players.LocalPlayer
    local runservice = game:GetService("RunService")

    local function esp(p, character)
        local humanoid = character:WaitForChild("Humanoid")
        local humanoidrootpart = character:WaitForChild("HumanoidRootPart")
        
        local text = Drawing.new("Text")
        text.Visible = false
        text.Center = true
        text.Outline = true
        text.Font = 3
        text.Color = Color3.fromRGB(40, 127, 71)
        text.Size = 13

        local c1
        local c2
        local c3
        
        local function disconnect()
            text.Visible = false
            text:Remove()
            if c1 then
                c1:Disconnect()
                c1 = nil
            end
            if c2 then
                c2:Disconnect()
                c2 = nil
            end
            if c3 then
                c3:Disconnect()
                c3 = nil
            end
        end
        
        c2 = character.AncestryChanged:Connect(function(_,parent)
            if not parent then
                disconnect()
            end
        end)
        
        c3 = humanoid.HealthChanged:Connect(function(v)
            pcall(function()
                if (v<=0) or (h:GetState() == Enum.HumanoidStateType.Dead) then
                    disconnect()
                end
            end)
        end)
        
        c1 = runservice.RenderStepped:Connect(function()
            local humanoidrootpart_pos,humanoidrootpart_onscreen = camera:WorldToViewportPoint(humanoidrootpart.Position)
            if humanoidrootpart_onscreen then
                text.Position = Vector2.new(humanoidrootpart_pos.X, humanoidrootpart_pos.Y-25)
                text.Text = p.Name
                text.Visible = true
            else
                text.Visible = false
            end
        end)
    end

    local function p_added(p)
        if p.Character then
            esp(p,p.Character)
        end
        p.CharacterAdded:Connect(function(character)
            esp(p,character)
        end)
    end

    for i,p in next, players:GetPlayers() do
        if p ~= localplayer then
            p_added(p)
        end
    end

    players.PlayerAdded:Connect(p_added)
end)

--VisualSection2
VisualSection2:NewButton("Gold Esp", "Gold Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(249, 233, 153)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Small Rock") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop["Small Rock"]["Position"])
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Gold"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Gold Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Gold Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end)
end)

VisualSection2:NewButton("Iron Esp", "Iron Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(217, 133, 108)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Small Rock") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop["Small Rock"]["Position"])
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Iron"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Iron Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Iron Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end)
end)

VisualSection2:NewButton("Crystal Esp", "Crystal Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(13, 105, 172)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Stone") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Stone.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Crystal"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Crystal Lode" then
            if drop:FindFirstChild("Stone") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Crystal Lode" then
            if drop:FindFirstChild("Stone") then
                esp(drop)
            end
        end
    end)
end)


VisualSection2:NewButton("Adurite Esp", "Adurite Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(196, 40, 28)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Small Rock") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop["Small Rock"]["Position"])
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Adurite"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Adurite Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Adurite Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end)
end)

VisualSection2:NewButton("Stone Esp", "Stone Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(161, 165, 162)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Small Rock") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop["Small Rock"]["Position"])
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Stone"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Stone Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Stone Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end)
end)

VisualSection2:NewButton("Coal Esp", "Coal Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(109, 110, 108)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Small Rock") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop["Small Rock"]["Position"])
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Coal"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Coal Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Coal Node" then
            if drop:FindFirstChild("Small Rock") then
                esp(drop)
            end
        end
    end)
end)

--VisualSection3
VisualSection3:NewButton("Old God Esp", "Old God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Old God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Old God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Old God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

VisualSection3:NewButton("Hateful God Esp", "Hateful God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Hateful God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Hateful God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Hateful God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

VisualSection3:NewButton("Sleeping God Esp", "Sleeping God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Sleeping God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Sleeping God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Sleeping God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

VisualSection3:NewButton("Wealthy God Esp", "Wealthy God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Wealthy God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Wealthy God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Wealthy God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

VisualSection3:NewButton("Lonely God Esp", "Lonely God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Lonely God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Lonely God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Lonely God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

VisualSection3:NewButton("Miserable God Esp", "Miserable God Esp", function()
    local camera = workspace.CurrentCamera
    local runservice = game:GetService("RunService")
    function esp(drop)
        local dropesp = Drawing.new("Text")
        dropesp.Visible = false
        dropesp.Center = true
        dropesp.Outline = true
        dropesp.Font = 3
        dropesp.Color = Color3.fromRGB(104, 92, 67)
        dropesp.Size = 13
        local renderstepped
        renderstepped = runservice.RenderStepped:Connect(function()
            if drop and workspace:FindFirstChild(drop.Name) and drop:FindFirstChild("Totem") then
                local drop_pos, drop_onscreen = camera:WorldToViewportPoint(drop.Totem.Position)
                if drop_onscreen then
                    dropesp.Position = Vector2.new(drop_pos.X, drop_pos.Y)
                    dropesp.Text = "Miserable God"
                    dropesp.Visible = true
                else
                    dropesp.Visible = false
                end
            else
                dropesp.Visible = false
                dropesp:Remove()
                renderstepped:Disconnect()
            end
        end)
    end
    for i,drop in next, workspace:GetChildren() do
        if drop.Name == "Miserable God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end
    workspace.ChildAdded:Connect(function()
        if drop.Name == "Miserable God" then
            if drop:FindFirstChild("Totem") then
                esp(drop)
            end
        end
    end)
end)

--MiscSection
MiscSection:NewButton("Execute Infinite Yield", "Infinite Yield", function()
    loadstring(game:HttpGet(('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'),true))()
end)

--ChangelogSection
ChangelogSection:NewLabel("1.0.0 | Esp, Gui Changes")
ChangelogSection:NewLabel("1.0.2 | Rebind Key To B (Bc Void Is V)")
ChangelogSection:NewLabel("1.0.3 | Fixed Childadded, Iron Esp")

--SettingSection
SettingSection:NewKeybind("Toggle Gui", "Toggles Gui", Enum.KeyCode.B, function()
	Library:ToggleUI()
end)
